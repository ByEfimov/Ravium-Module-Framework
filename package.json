{
  "name": "ravium-module-framework",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.22.0",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' run build",
    "test": "pnpm --filter @ravium/cli run test",
    "check": "pnpm run build && pnpm run test",
    "publish:public": "pnpm run check && pnpm -r --filter './packages/*' publish --access public --no-git-checks"
  },
  "devDependencies": {
    "@types/node": "^25.3.2",
    "typescript": "^5.7.2"
  }
}
